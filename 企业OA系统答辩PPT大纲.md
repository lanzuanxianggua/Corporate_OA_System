# 企业 OA 办公系统项目答辩 PPT

## 第 1 页：封面
**标题**: 企业 OA 办公自动化系统  
**副标题**: 基于 Spring Boot + Vue 3 的企业级办公管理平台  
**答辩人**: [姓名]  
**日期**: 2026 年 6 月

---

## 第 2 页：项目概述
### 系统定位
- 面向 200-5000 人企业的办公自动化系统
- 覆盖行政、人事、财务、审批、协同、资产、报表 8 大业务域
- 减少线下审批和人工统计成本，将日常办公流程线上化

### 核心目标
- 业务模块清晰
- 审批流程可配置
- 权限可控、数据可追溯
- 接口统一、前端页面可快速扩展

---

## 第 3 页:技术架构
```
┌─────────────┐
│  浏览器      │
└──────┬──────┘
       │ REST API + JWT + WebSocket
┌──────▼──────┐
│  Vue 3 前端  │ TypeScript + Vite + Element Plus
└──────┬──────┘
       │
┌──────▼──────┐
│Spring Boot后端│ Java 17 + MyBatis-Plus
└──────┬──────┘
       │
┌──────▼──────┐
│ MySQL + Redis│
└─────────────┘
```

---

## 第 4 页：技术选型

### 前端技术栈
| 技术 | 用途 | 优势 |
|------|------|------|
| Vue 3 + TypeScript | SPA 框架 | Composition API 适合复杂表单 |
| Element Plus | UI 组件库 | 管理后台组件齐全 |
| Vite | 构建工具 | 启动快、热更新快 |
| Pinia | 状态管理 | 轻量、易用 |
| ECharts | 数据可视化 | 支持丰富的图表类型 |

### 后端技术栈
| 技术 | 用途 | 优势 |
|------|------|------|
| Spring Boot 3.4 | 应用框架 | 自动配置完善 |
| MyBatis-Plus | ORM | 减少重复 CRUD 代码 |
| MySQL 8 | 关系型数据库 | 适合事务、报表和状态查询 |
| Redis | 缓存 | Token、角色缓存、在线状态 |
| JWT | 登录令牌 | 前后端分离下的无状态认证 |

---

## 第 5 页：后端架构设计

### Maven 模块架构（5 个核心模块）
```
oa-web (启动模块)
  ├── oa-common   (通用能力: R<T>、异常、JWT、Redis、拦截器)
  ├── oa-model    (Entity、DTO、VO 数据模型)
  ├── oa-mapper   (MyBatis-Plus Mapper 数据访问层)
  └── oa-service  (核心业务逻辑、事务、审批、通知)
```

### 分层架构
- **Controller 层**: 接收请求、参数校验、调用 Service、返回 `R<T>`
- **Service 层**: 业务校验、事务、审批流、状态变更、统计聚合
- **Mapper 层**: 数据库访问（继承 MyBatis-Plus `BaseMapper`）
- **Model 层**: Entity 映射表、DTO 接收参数、VO 输出数据

---

## 第 6 页：数据库设计

### 数据库分域（5 大类）
| 分域 | 表名前缀 | 典型表 |
|------|----------|--------|
| 系统基础 | `sys_` | 员工、部门、角色、菜单、字典、参数、岗位 |
| OA 业务 | `oa_` | 考勤、请假、公告、消息、文档、资产、会议、合同、预算 |
| 工作流 | `wf_` | 流程定义、流程实例、审批任务、抄送、委托 |
| 报表预警 | `rpt_` | 预警规则、预警日志 |
| 扩展业务 | `adm_`/`hr_`/`fin_` | 用品、绩效、招聘、培训、知识库、任务、财务 |

### 通用字段设计
- `id` (主键)、`create_by`/`update_by` (审计)
- `create_time`/`update_time` (时间戳)
- `del_flag` (逻辑删除)、`status` (业务状态)
- `version` (乐观锁)

---

## 第 7 页：核心功能模块（1）

### 1. 登录认证模块
- 验证码 + JWT Token
- Redis 保存 Token（支持服务端主动失效）
- IP 限流防暴力破解
- 在线用户管理、强制下线

### 2. 权限与系统管理
- **RBAC 权限模型**: 用户-角色-菜单
- 部门树、岗位管理
- 字典类型、参数配置
- 菜单路由、权限标识

### 3. 考勤管理
- 上下班打卡（记录 IP 和地址）
- 考勤组（配置上班时间、迟到阈值）
- 假期余额（年度假期初始化、审批扣减）
- 考勤记录查询、导出

---

## 第 8 页：核心功能模块（2）

### 4. 审批中心与工作流引擎（⭐️ 核心亮点）
- **自研轻量级工作流引擎**
  - 支持流程图 JSON 配置
  - 条件路由（金额、天数、小时数分级审批）
  - 超时提醒、自动通过/驳回、升级审批
- **审批功能**
  - 待办/已办任务
  - 审批时间线
  - 转办、委托、催办、撤回
  - 抄送我的

### 5. 审批业务（统一模式）
- 请假、出差、外出、加班
- 报销、采购、借支
- 统一审批流程、状态联动

---

## 第 9 页：核心功能模块（3）

### 6. 办公协同
- **通知公告**: 发布公告、阅读记录
- **文档中心**: 分类管理、上传下载、下载计数
- **内部消息**: 点对点消息、未读统计
- **会议管理**: 会议室预约、冲突检查
- **日程管理**: 个人日程、全局总览
- **工作台**: 聚合待办、消息、公告、日程

### 7. 资产、合同、预算
- 资产台账、资产借还
- 合同管理（编号、双方、金额、到期提醒）
- 部门预算（按年月维护、已用金额统计）

---

## 第 10 页：核心功能模块（4）

### 8. 人事、薪资、绩效、招聘、培训
- **员工档案**: 教育经历、入职时间、合同期限
- **薪资管理**: 薪资结构、薪资记录
- **绩效管理**: 目标制定、评价提交、结果生成
- **招聘管理**: 岗位、候选人、面试、Offer
- **培训管理**: 课程、计划、场次、报名、签到、评分

### 9. 财务、知识库、任务协作、办公用品
- 财务合同、付款管理
- 知识库分类、条目发布
- 任务项目、进度更新
- 办公用品库存、领用申请

---

## 第 11 页：工作流引擎设计（⭐️ 技术亮点）

### 设计思路
- **业务解耦**: 业务表只保存单据，审批运行态放在 `wf_*` 表
- **流程定义**: JSON 格式保存流程图（节点、网关、审批人、超时动作）
- **流程实例**: `business_type + business_id` 关联任意业务单据
- **流程快照**: 保存流程定义快照，防止新流程影响历史审批

### 核心能力
- 条件路由（金额 > 5000 → 总监审批）
- 超时机制（自动提醒、自动通过、自动驳回、升级审批）
- 审批委托（设置代理人）
- 转办、催办、撤回

### 实现价值
- **一套工作流支持多种业务**（请假、报销、采购等）
- **流程可配置**（无需改代码）
- **审批历史可追溯**

---

## 第 12 页：安全与权限设计

### 1. 登录安全
- 密码加密校验
- 验证码 + IP 限流
- Token 存 Redis（服务端可主动失效）

### 2. 接口鉴权
- `AuthInterceptor` 统一拦截
- 解析 JWT + 校验 Redis Token
- 注解式权限: `@RequireAdmin`、`@RequireRole`、`@RequirePermission`

### 3. 前端权限
- 路由 `meta.roles` 控制页面访问
- 菜单 `roles` 控制显示
- 前端权限用于体验，后端权限用于安全

---

## 第 13 页：通知与实时能力

### 三种提醒渠道
1. **待办表 `oa_todo`**: 可查询、可完成的任务提醒
2. **消息表 `oa_message`**: 内部消息和未读状态
3. **WebSocket**: 实时推送审批结果、待办和消息

### 设计原因
- WebSocket 只负责实时性，不适合做唯一存储
- 待办和消息落库后，即使用户离线也不会丢失
- 前端顶部未读数 = 定时查询 + WebSocket 增量更新

---

## 第 14 页：报表与数据看板

### 个人报表
- 个人考勤汇总、考勤趋势
- 请假统计、月度对比

### 管理报表
- 部门考勤对比、考勤趋势
- 请假分析、员工排名
- 今日概览

### 数据看板
- 考勤、审批、业务指标汇总
- 趋势图、漏斗图、热力图（ECharts）

### 预警机制
- 预警规则配置（指标、阈值、通知方式）
- 预警日志记录、处理闭环

---

## 第 15 页：项目亮点与创新点

### ⭐️ 技术亮点
1. **自研轻量级工作流引擎**
   - JSON 配置化流程
   - 支持条件路由、超时机制、审批委托
   - 一套引擎支持多种业务

2. **统一审批抽象**
   - `BaseApprovalServiceImpl` 抽象通用审批流程
   - 减少 70% 重复代码

3. **实时通知架构**
   - WebSocket + 待办表 + 消息表
   - 兼顾实时性和可靠性

### 🏆 业务亮点
1. **业务覆盖全面**: 8 大业务域，50+ 功能模块
2. **权限体系完善**: RBAC + 数据权限 + 操作日志
3. **数据可追溯**: 逻辑删除 + 审计字段 + 审批历史

---

## 第 16 页：系统特色

### 1. 架构清晰
- 前后端分离、分层架构
- 模块边界清晰（系统、业务、流程、报表分域）

### 2. 可扩展性强
- 新增业务表 → 启动流程即可接入审批
- 字典表、配置表将枚举从代码中抽离
- 前端组件和 API 按模块拆分

### 3. 运维友好
- 环境变量配置（避免生产密码硬编码）
- 操作日志 AOP（统一记录，不侵入业务）
- 在线用户管理（强制下线）

---

## 第 17 页：开发与测试

### 开发环境
- **后端**: `localhost:8080`
- **前端**: `localhost:8848`
- **数据库**: `oa_system`
- **Redis**: `127.0.0.1:6379`

### 测试覆盖
- **后端测试**: Controller + Service 关键逻辑
  - 登录认证、审批流程、考勤打卡
- **前端测试**:
  - 单元测试（API 调用参数验证）
  - E2E 测试（登录页渲染）

### 构建验证
- 前端通过类型检查和生产构建
- 后端通过全量编译和测试

---

## 第 18 页：系统演示截图

### 核心页面
1. 登录页
2. 工作台（待办、消息、公告、日程）
3. 考勤打卡
4. 审批中心（待办任务）
5. 流程设计器
6. 数据看板

*（建议插入实际系统截图）*

---

## 第 19 页：后续优化方向

### 可优化点
1. **动态路由**: 从菜单表动态生成路由
2. **API 路径统一**: 统一为 `/api/system/*`
3. **类型增强**: 减少 `any`，增强 DTO/VO 类型
4. **前端性能**: ECharts 按页面懒加载
5. **对象存储**: 文件从本地目录迁移到 OSS
6. **流程引擎升级**: 复杂流程可接入 BPMN 标准（Flowable/Camunda）

### 扩展方向
- 移动端 H5/小程序
- 多租户支持
- 国际化 i18n
- 分布式事务

---

## 第 20 页：项目总结

### 项目成果
- ✅ 完成 8 大业务域、50+ 功能模块
- ✅ 自研工作流引擎，支持可配置审批流程
- ✅ 前后端分离架构，代码结构清晰
- ✅ RBAC 权限模型，数据可追溯
- ✅ 实时通知、报表看板、预警机制

### 技术收获
- 深入理解 Spring Boot 3 + MyBatis-Plus
- 掌握 Vue 3 Composition API + TypeScript
- 理解工作流引擎设计思想
- 掌握前后端分离架构的安全与权限设计

### 适用场景
- 中小型企业 OA 系统
- 毕业设计、课程设计
- 内部管理系统原型

---

## 第 21 页：致谢

感谢各位老师的指导与帮助！

欢迎提问与建议 🙏
