# 企业OA办公系统 — 项目说明书

## 一、项目概述

本系统是一个企业内部OA（Office Automation）办公系统，采用前后端分离架构，分为**员工端**和**管理员端**两个角色视角。系统覆盖考勤管理、请假审批、公告通知、文档管理、日程安排、消息中心、数据报表等核心办公场景。

| 层 | 技术 | 版本 |
|---|---|---|
| 后端框架 | Spring Boot | 3.4.5 |
| ORM | MyBatis-Plus | 3.5.9 |
| 数据库 | MySQL | 8.0 |
| 缓存 | Redis | — |
| 认证 | JWT (jjwt) | 0.12.5 |
| API文档 | Knife4j (OpenAPI3) | 4.5.0 |
| 前端框架 | Vue 3 + TypeScript | — |
| UI组件 | Element Plus | — |
| 样式 | Tailwind CSS 4 | — |
| 路由 | Vue Router 5 | — |
| 状态管理 | Pinia | — |
| 构建工具 | Vite 6 | — |

---

## 二、数据库设计

### 2.1 设计原则

1. **业务分离**：系统管理相关的表使用 `sys_` 前缀，OA业务相关的表使用 `oa_` 前缀，职责清晰。
2. **雪花ID**：所有主键采用 MyBatis-Plus 的 `ASSIGN_ID` 策略（雪花算法），生成分布式唯一 Long 型 ID，避免自增ID在分库分表场景下的冲突问题。
3. **软关联**：通过 `empId`、`deptId` 等外键字段进行逻辑关联，不使用数据库物理外键约束。这样做的好处是：避免外键导致的级联锁问题，提升写入性能，同时业务层保证数据一致性。
4. **自动填充**：`createTime`、`updateTime` 等时间字段通过 MyBatis-Plus 的 `@TableField(fill = FieldFill.INSERT)` 注解自动填充，减少重复代码。
5. **状态字段统一**：所有状态字段统一使用 `Integer` 类型，`1` 表示正常/启用，`0` 表示禁用/待审批，各业务表的状态含义由业务层定义。

### 2.2 数据库表总览（共15张表）

| 分类 | 表名 | 说明 |
|------|------|------|
| 系统管理 | `sys_employee` | 员工表 |
| 系统管理 | `sys_dept` | 部门表 |
| 系统管理 | `sys_role` | 角色表 |
| 系统管理 | `sys_emp_role` | 员工-角色关联表 |
| OA业务 | `oa_attendance` | 考勤记录表 |
| OA业务 | `oa_leave_apply` | 请假申请表 |
| OA业务 | `oa_approval_record` | 审批记录表 |
| OA业务 | `oa_notice` | 通知公告表 |
| OA业务 | `oa_document` | 文档管理表 |
| OA业务 | `oa_schedule` | 日程安排表 |
| OA业务 | `oa_message` | 站内消息表 |
| 系统监控 | `oa_operation_log` | 操作日志表 |
| 系统监控 | `oa_login_log` | 登录日志表 |

### 2.3 各表字段详解

---

#### 2.3.1 sys_employee（员工表）

存储系统所有员工的基本信息和登录凭证。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | bigint | 主键，雪花算法生成 |
| `emp_code` | varchar | 员工工号，如 "EMP001"，登录时使用此字段 |
| `emp_name` | varchar | 员工真实姓名 |
| `password` | varchar | 登录密码，BCrypt 加密存储 |
| `phone` | varchar | 手机号码 |
| `email` | varchar | 邮箱地址 |
| `dept_id` | bigint | 所属部门ID，关联 sys_dept.id |
| `avatar` | varchar | 头像路径 |
| `status` | int | 账号状态：1=启用，0=禁用 |
| `create_time` | datetime | 创建时间，自动填充 |
| `update_time` | datetime | 更新时间，自动填充 |

**设计理由**：员工表是系统核心表，几乎所有业务表都通过 `emp_id` 关联到此表。密码使用 BCrypt 加密而非 MD5，是因为 BCrypt 自带盐值且计算慢，能有效防止彩虹表攻击。`status` 字段支持软删除和账号停用，避免物理删除导致关联数据断裂。

---

#### 2.3.2 sys_dept（部门表）

存储公司组织架构，采用 `parent_id` 自引用的树形结构设计。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | bigint | 主键 |
| `dept_name` | varchar | 部门名称，如 "技术部" |
| `parent_id` | bigint | 父部门ID，顶级部门为 0 |
| `sort` | int | 显示排序号 |
| `leader` | varchar | 部门负责人姓名 |
| `phone` | varchar | 部门联系电话 |
| `status` | int | 状态：1=启用，0=禁用 |
| `create_time` | datetime | 创建时间 |
| `update_time` | datetime | 更新时间 |

**设计理由**：采用 `parent_id` 自引用实现树形结构（邻接表模型），而非单独建一张层级关系表。原因：OA系统的部门层级通常不超过4-5层，递归查询性能完全足够；且这种设计简单直观，增删改都只需操作单条记录。`sort` 字段控制同级部门的显示顺序。

---

#### 2.3.3 sys_role（角色表）

定义系统角色，目前有 ADMIN（管理员）和 USER（普通员工）两种角色。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | bigint | 主键 |
| `role_name` | varchar | 角色名称，如 "管理员"、"普通员工" |
| `role_key` | varchar | 角色标识，如 "admin"、"user"，用于权限判断 |
| `sort` | int | 排序号 |
| `status` | int | 状态：1=启用，0=禁用 |
| `create_time` | datetime | 创建时间 |

**设计理由**：`role_key` 是程序中权限判断的依据（如 `roles: ["admin"]`），与 `role_name` 分离，这样修改角色显示名不影响权限逻辑。采用 RBAC（基于角色的访问控制）模型，而非直接给员工分配权限，便于批量管理。

---

#### 2.3.4 sys_emp_role（员工-角色关联表）

实现员工与角色的多对多关系。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | bigint | 主键 |
| `emp_id` | bigint | 员工ID，关联 sys_employee.id |
| `role_id` | bigint | 角色ID，关联 sys_role.id |

**设计理由**：独立建关联表而非在员工表加 `role_id` 字段，是因为一个员工可能同时拥有多个角色（如既是部门经理又是系统管理员）。多对多关系是 RBAC 模型的标准设计。

---

#### 2.3.5 oa_attendance（考勤记录表）

记录每位员工每天的打卡数据。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | bigint | 主键 |
| `emp_id` | bigint | 员工ID |
| `work_date` | date | 工作日期，如 "2026-05-16" |
| `clock_in` | datetime | 上班打卡时间 |
| `clock_out` | datetime | 下班打卡时间 |
| `status` | int | 考勤状态：0=正常，1=迟到，2=早退，3=缺勤，4=请假 |
| `remark` | varchar | 备注 |

**设计理由**：以 `(emp_id, work_date)` 为业务唯一键，一个员工一天只有一条考勤记录。`status` 使用数字编码而非字符串，节省存储空间且索引效率更高。打卡时间使用 `datetime` 而非 `time`，因为需要精确记录到秒，且后续计算工作时长需要完整的时间戳。

---

#### 2.3.6 oa_leave_apply（请假申请表）

记录员工的请假申请信息。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | bigint | 主键 |
| `emp_id` | bigint | 申请员工ID |
| `leave_type` | int | 请假类型：0=事假，1=病假，2=年假，3=婚假，4=产假，5=其他 |
| `start_time` | datetime | 请假开始时间 |
| `end_time` | datetime | 请假结束时间 |
| `reason` | varchar | 请假原因 |
| `status` | int | 审批状态：0=待审批，1=已通过，2=已拒绝 |
| `create_time` | datetime | 申请时间 |

**设计理由**：`leave_type` 使用数字枚举，通过业务层映射为中文含义，便于扩展新类型。审批状态设计为三态（待审/通过/拒绝），覆盖完整的审批流程。请假时间使用 `datetime` 而非 `date`，支持半天请假等精确场景。

---

#### 2.3.7 oa_approval_record（审批记录表）

记录每次审批操作的详细信息，支持多次审批历史追溯。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | bigint | 主键 |
| `apply_id` | bigint | 关联的申请单ID（如请假申请） |
| `approver_id` | bigint | 审批人ID |
| `approve_status` | int | 审批结果：1=通过，2=拒绝 |
| `remark` | varchar | 审批意见 |
| `approve_time` | datetime | 审批时间 |

**设计理由**：独立建审批记录表而非在请假表加审批字段，原因是：一条请假申请可能被多次审批（如一审、二审），审批记录表可以完整保留审批链路。同时，审批记录表不绑定具体业务，`apply_id` 可以关联任何类型的申请单，未来扩展报销申请、加班申请等不需要新建审批表。

---

#### 2.3.8 oa_notice（通知公告表）

存储管理员发布的公告和通知。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | bigint | 主键 |
| `title` | varchar | 公告标题 |
| `content` | text | 公告正文内容 |
| `notice_type` | int | 公告类型：0=通知，1=公告 |
| `publisher_id` | bigint | 发布人ID |
| `status` | int | 状态：0=草稿，1=已发布 |
| `create_time` | datetime | 创建时间 |
| `update_time` | datetime | 更新时间 |

**设计理由**：公告的已读状态不在此表记录，而是通过 Redis Set 存储（详见 Redis 章节说明）。这样做的理由：已读状态是高频率写入操作（每个员工打开每条公告都要标记），如果存在数据库表中会产生大量记录（公告数 × 员工数），而 Redis Set 的 `SISMEMBER` 操作可以在 O(1) 时间复杂度内判断是否已读。

---

#### 2.3.9 oa_document（文档管理表）

存储上传文档的元数据信息，实际文件保存在服务器磁盘。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | bigint | 主键 |
| `doc_name` | varchar | 原始文件名，如 "需求文档.docx" |
| `file_path` | varchar | 服务器存储路径（UUID重命名后的文件名） |
| `file_size` | bigint | 文件大小（字节） |
| `file_type` | varchar | 文件扩展名，如 ".docx"、".pdf" |
| `category_id` | bigint | 所属分类ID |
| `uploader_id` | bigint | 上传人ID |
| `create_time` | datetime | 上传时间 |

**设计理由**：文件采用 UUID 重命名存储而非使用原始文件名，原因有三：避免中文文件名导致的编码问题；避免同名文件覆盖；防止路径遍历攻击。数据库只存元数据，文件本身存在磁盘上，这是文档管理系统的标准做法——数据库存储大文件会严重影响性能。

---

#### 2.3.10 oa_schedule（日程安排表）

员工的个人日程安排。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | bigint | 主键 |
| `emp_id` | bigint | 日程所属员工ID |
| `title` | varchar | 日程标题 |
| `content` | varchar | 日程详细内容 |
| `start_time` | datetime | 开始时间 |
| `end_time` | datetime | 结束时间 |
| `remind_time` | datetime | 提醒时间（预留字段） |
| `status` | int | 日程状态：0=未开始，1=已完成等 |
| `create_time` | datetime | 创建时间 |

**设计理由**：日程表的 `emp_id` 确保每个员工只能看到自己的日程（管理员可查看所有）。`remind_time` 字段为日程提醒功能预留，可通过定时任务扫描即将到期的日程并发送通知。

---

#### 2.3.11 oa_message（站内消息表）

员工之间的站内消息。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | bigint | 主键 |
| `sender_id` | bigint | 发送人ID |
| `receiver_id` | bigint | 接收人ID |
| `msg_type` | int | 消息类型：0=系统消息，1=个人消息 |
| `title` | varchar | 消息标题 |
| `content` | varchar | 消息内容 |
| `is_read` | int | 已读状态：0=未读，1=已读 |
| `create_time` | datetime | 发送时间 |

**设计理由**：消息采用点对点模式（sender → receiver），`msg_type` 区分系统消息和个人消息。系统消息由后端自动生成（如审批通过通知），个人消息由员工手动发送。未读消息计数通过 Redis 缓存，避免每次查询都 COUNT 数据库。

---

#### 2.3.12 oa_operation_log（操作日志表）

记录系统中所有重要操作，通过 AOP 切面自动采集。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | bigint | 主键 |
| `emp_id` | bigint | 操作人ID |
| `emp_name` | varchar | 操作人姓名（冗余存储） |
| `module` | varchar | 操作模块，如 "考勤管理" |
| `operation` | varchar | 操作描述，如 "上班打卡" |
| `method` | varchar | Java方法全路径 |
| `request_url` | varchar | 请求URL |
| `ip` | varchar | 操作者IP地址 |
| `status` | int | 操作结果：1=成功，0=失败 |
| `cost_time` | bigint | 接口耗时（毫秒） |
| `create_time` | datetime | 操作时间 |

**设计理由**：通过 `@OperationLog` 注解 + AOP 切面实现，业务代码零侵入。`emp_name` 冗余存储是因为员工可能离职改名，日志数据应保留操作时的状态。`cost_time` 字段用于接口性能分析，帮助发现慢接口。

---

#### 2.3.13 oa_login_log（登录日志表）

记录每次登录行为的详细信息。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | bigint | 主键 |
| `emp_id` | bigint | 登录员工ID |
| `username` | varchar | 登录使用的用户名 |
| `ip` | varchar | 登录IP地址 |
| `browser` | varchar | 浏览器信息 |
| `os` | varchar | 操作系统信息 |
| `status` | int | 登录结果：1=成功，0=失败 |
| `message` | varchar | 结果消息，如 "登录成功" |
| `login_time` | datetime | 登录时间 |

**设计理由**：登录日志独立于操作日志，因为登录请求不经过 JWT 拦截器（登录接口已排除），无法使用同一个 AOP 切面采集。同时登录日志关注的安全维度不同（IP、浏览器、是否成功），适合单独管理。可用于安全审计，如检测异常登录行为。

---

### 2.4 表关系图

```
sys_dept  1──N  sys_employee  N──N  sys_role
  (parent_id自引用树)     |         (通过sys_emp_role关联)
                         |
            ┌────────────┼────────────┬────────────┐
            |            |            |            |
      oa_attendance  oa_leave_apply  oa_schedule  oa_message
            |            |            |           (sender/receiver)
            |      oa_approval_record
            |
      oa_notice ──(Redis Set)── 已读记录
            |
      oa_document

      oa_operation_log    oa_login_log
        (AOP自动采集)      (登录时记录)
```

---

## 三、后端接口设计

### 3.1 设计原则

1. **RESTful 风格**：资源路径使用名词（如 `/api/attendance`），操作通过 HTTP 方法区分（GET 查询、POST 新增/操作、PUT 修改、DELETE 删除）。
2. **统一响应格式**：所有接口返回 `R<T>` 包装类，包含 `code`（0=成功，-1=失败，401=未授权）、`message`、`data` 三个字段。前端统一判断 `code === 0` 确定请求是否成功。
3. **JWT 认证**：除登录、刷新Token等接口外，所有请求需携带 `Authorization: Bearer <token>` 请求头。`AuthInterceptor` 拦截器统一校验 Token 有效性。
4. **分页标准化**：所有分页接口统一使用 `pageNum`（页码）、`pageSize`（每页条数）参数，返回 `PageResult` 对象（包含 `total` 总数和 `list` 数据列表）。
5. **角色隔离**：管理员接口（如考勤管理、请假审批）通过后端路由的 `roles` 字段控制前端菜单可见性，同时 Controller 层也可结合 `empId` 进行数据权限控制。

### 3.2 接口清单（共65个接口，15个Controller）

---

#### 3.2.1 AuthController — 认证模块（3个接口）

| 方法 | 路径 | 说明 | 设计理由 |
|------|------|------|----------|
| POST | `/login` | 用户登录 | 返回 accessToken + refreshToken，accessToken 用于日常请求，refreshToken 用于无感续期 |
| POST | `/refresh-token` | 刷新Token | accessToken 过期后前端自动调用此接口换取新 Token，避免用户频繁重新登录 |
| POST | `/logout` | 用户登出 | 同时删除 Redis 中的 Token 记录和在线用户信息，确保已登出的 Token 立即失效 |

**为什么这样设计**：双 Token 机制（accessToken + refreshToken）是业界标准做法。accessToken 有效期短（2小时），即使泄露影响有限；refreshToken 用于续期，减少用户登录次数。登出时从 Redis 删除 Token，实现服务端主动使 Token 失效的能力。

---

#### 3.2.2 AttendanceController — 考勤模块（5个接口）

| 方法 | 路径 | 说明 | 设计理由 |
|------|------|------|----------|
| POST | `/api/attendance/clock-in` | 上班打卡 | 从 JWT 中取 empId，防止伪造他人打卡 |
| POST | `/api/attendance/clock-out` | 下班打卡 | 同上，且校验必须先签到才能签退 |
| GET | `/api/attendance/today` | 获取今日考勤 | 工作台/考勤页展示今日打卡状态 |
| GET | `/api/attendance/history` | 考勤历史查询 | 员工查看自己的历史考勤，通过日期范围筛选 |
| GET | `/api/attendance/admin/page` | 管理员考勤分页 | 管理员查看全员考勤，支持按姓名、状态、日期筛选 |

**为什么这样设计**：打卡接口用 POST 因为是写操作（创建考勤记录）。empId 不由前端传入，而是从 JWT Token 中解析，防止用户伪造身份替别人打卡。管理员接口单独设计 `/admin/page` 路径而非复用普通查询接口，因为两者查询维度完全不同——员工只查自己、管理员查全员且需关联员工姓名和部门。

---

#### 3.2.3 LeaveApplyController — 请假模块（3个接口）

| 方法 | 路径 | 说明 | 设计理由 |
|------|------|------|----------|
| POST | `/api/leave/submit` | 提交请假 | 员工发起请假，status 默认 0（待审批） |
| POST | `/api/leave/approve` | 审批请假 | 管理员审批，同时写入审批记录表 |
| GET | `/api/leave/page` | 分页查询 | 员工查自己的，管理员查所有人的，通过 empId 参数区分 |

**为什么这样设计**：审批是核心业务流程，`approve` 接口同时操作两张表（更新请假状态 + 插入审批记录），用 `@Transactional` 保证事务一致性。分页查询通过可选的 `empId` 和 `status` 参数灵活支持多种场景：员工查自己的待审/已审记录、管理员查所有待审批记录。

---

#### 3.2.4 NoticeController — 公告模块（6个接口）

| 方法 | 路径 | 说明 | 设计理由 |
|------|------|------|----------|
| GET | `/api/notice/page` | 分页查询公告 | 所有用户可查看已发布公告 |
| GET | `/api/notice/{id}` | 获取公告详情 | 点击公告时查看全文 |
| POST | `/api/notice` | 新增公告 | 仅管理员操作 |
| PUT | `/api/notice` | 修改公告 | 仅管理员操作 |
| DELETE | `/api/notice/{id}` | 删除公告 | 仅管理员操作 |
| POST | `/api/notice/{id}/read` | 标记已读 | 将用户ID加入 Redis Set |

**为什么这样设计**：公告模块采用标准 CRUD 五件套（page/get/add/update/delete）加上已读标记接口。已读标记使用 POST 方法因为它是写操作（向 Redis Set 添加成员）。CRUD 操作对资源类型做了区分——公告的增删改只有管理员可以执行，查询所有用户可见。

---

#### 3.2.5 DocumentController — 文档模块（3个接口）

| 方法 | 路径 | 说明 | 设计理由 |
|------|------|------|----------|
| GET | `/api/document/page` | 分页查询文档 | 查看文档列表 |
| POST | `/api/document/upload` | 上传文档 | multipart/form-data 格式，接收文件+上传人ID |
| DELETE | `/api/document/{id}` | 删除文档 | 删除数据库记录，磁盘文件保留 |

**为什么这样设计**：上传接口使用 `multipart/form-data` 而非 Base64 编码，原因是文件上传场景下 multipart 更高效——不需要编解码转换，内存占用更小，且 Spring Boot 原生支持。`uploaderId` 由前端传入而非从 Token 取，因为管理员可能代他人上传文档。

---

#### 3.2.6 ScheduleController — 日程模块（4个接口）

| 方法 | 路径 | 说明 | 设计理由 |
|------|------|------|----------|
| GET | `/api/schedule/page` | 分页查询日程 | 支持按 empId 筛选 |
| POST | `/api/schedule` | 新增日程 | — |
| PUT | `/api/schedule` | 修改日程 | — |
| DELETE | `/api/schedule/{id}` | 删除日程 | — |

**为什么这样设计**：日程是典型的个人 CRUD 业务，四个接口对应增删改查四种操作。标准 RESTful 设计，无特殊业务逻辑。

---

#### 3.2.7 MessageController — 消息模块（3个接口）

| 方法 | 路径 | 说明 | 设计理由 |
|------|------|------|----------|
| GET | `/api/message/unread-count` | 获取未读消息数 | 工作台展示未读消息徽标 |
| POST | `/api/message/send` | 发送消息 | 管理员向指定员工发送消息 |
| POST | `/api/message/{id}/read` | 标记已读 | 切换消息状态为已读 |

**为什么这样设计**：`unread-count` 是高频调用接口（工作台每次加载都要调用），返回一个简单的数字，数据量极小。消息列表使用分页接口在 MonitorController 中统一提供。发送消息时同时更新 Redis 中的未读计数缓存。

---

#### 3.2.8 ReportController — 报表模块（10个接口）

**个人报表（4个）：**

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/report/personal/attendance-summary` | 个人月度考勤汇总（正常/迟到/早退/缺勤天数） |
| GET | `/api/report/personal/attendance-trend` | 个人出勤趋势（近N个月） |
| GET | `/api/report/personal/leave-summary` | 个人请假统计（按类型分组） |
| GET | `/api/report/personal/monthly-compare` | 个人月度对比（与上月比） |

**管理员报表（6个）：**

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/report/admin/attendance-summary` | 全员考勤汇总 |
| GET | `/api/report/admin/dept-compare` | 各部门出勤率对比 |
| GET | `/api/report/admin/attendance-trend` | 全员出勤趋势 |
| GET | `/api/report/admin/leave-analysis` | 请假类型分析（饼图数据） |
| GET | `/api/report/admin/employee-ranking` | 员工出勤排名（红榜/黑榜） |
| GET | `/api/report/admin/today-overview` | 今日打卡概览 |

**为什么这样设计**：报表接口按个人/管理员两个维度拆分，因为两者的数据聚合范围完全不同。个人报表只涉及单个员工的数据，管理员报表涉及全员聚合。每个接口返回特定格式的数据，直接映射到前端图表组件（折线图、饼图、柱状图等）。所有报表数据都有 Redis 缓存，因为聚合查询计算量大但数据更新频率低。

---

#### 3.2.9 StatisticsController — 数据统计（1个接口）

| 方法 | 路径 | 说明 | 设计理由 |
|------|------|------|----------|
| GET | `/api/statistics/dashboard` | 获取仪表盘数据 | 支持 period 参数（today/week/month），返回对应时段的统计数据 |

**为什么这样设计**：仪表盘是管理员首页，需要展示多维度统计数据（出勤人数、请假人数、各部门出勤率等）。将这些数据聚合在一个接口中返回，减少前端请求次数。通过 `period` 参数切换时间维度，后端统一处理时间范围计算。

---

#### 3.2.10 EmployeeController — 员工管理（6个接口）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/employee/page` | 分页查询员工（支持按姓名/部门筛选） |
| GET | `/api/employee/{id}` | 获取员工详情 |
| POST | `/api/employee` | 新增员工（密码默认BCrypt加密） |
| PUT | `/api/employee` | 修改员工信息 |
| DELETE | `/api/employee/{id}` | 删除员工 |
| PUT | `/api/employee/password` | 修改密码（需验证旧密码） |

**为什么这样设计**：修改密码需要单独接口而非复用修改接口，因为修改密码需要验证旧密码（安全要求），且不需要传入其他员工字段。

---

#### 3.2.11 DeptController — 部门管理（4个接口）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/dept/tree` | 获取部门树 |
| POST | `/api/dept` | 新增部门 |
| PUT | `/api/dept` | 修改部门 |
| DELETE | `/api/dept/{id}` | 删除部门 |

**为什么这样设计**：部门查询接口返回树形结构（`children` 嵌套），而非平面列表，因为前端组件（如 el-tree）直接消费树形数据。后端递归构建树，前端无需二次处理。

---

#### 3.2.12 RouteController — 动态路由（1个接口）

| 方法 | 路径 | 说明 | 设计理由 |
|------|------|------|----------|
| GET | `/get-async-routes` | 获取前端路由配置 | 根据用户角色返回不同的菜单结构 |

**为什么这样设计**：动态路由实现前端菜单的权限控制。管理员看到管理端菜单（考勤管理、请假审批、数据看板等），普通员工只看到员工端菜单（我的考勤、请假申请等）。路由配置由后端返回而非前端硬编码，便于动态调整菜单结构而无需重新部署前端。

---

#### 3.2.13 SystemManageController — 系统管理（10个接口）

提供用户管理、角色管理、菜单管理、个人信息等系统级功能。

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/user` | 分页查询用户 |
| GET | `/list-all-role` | 获取所有角色列表 |
| POST | `/list-role-ids` | 获取用户拥有的角色ID |
| POST | `/role` | 分页查询角色 |
| POST | `/menu` | 获取菜单列表 |
| POST | `/dept` | 获取部门列表 |
| POST | `/role-menu` | 获取角色菜单关联 |
| POST | `/role-menu-ids` | 获取角色拥有的菜单ID |
| GET | `/mine` | 获取当前登录用户信息 |
| GET | `/mine-logs` | 获取当前用户操作日志 |

**为什么这样设计**：系统管理接口路径不带 `/api` 前缀，因为这部分接口来源于 vue-pure-admin 框架的原生接口约定，前端框架对这些路径有硬编码依赖，保持兼容。

---

#### 3.2.14 MonitorController — 系统监控（5个接口）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/online-logs` | 查询在线用户 |
| POST | `/login-logs` | 查询登录日志 |
| POST | `/operation-logs` | 查询操作日志 |
| POST | `/system-logs` | 查询系统日志 |
| POST | `/system-logs-detail` | 查看日志详情 |

**为什么这样设计**：监控接口统一使用 POST 方法接收查询参数（而非 GET + query string），是因为查询条件可能较复杂，POST body 更灵活。在线用户数据从 Redis 中读取（`online:user:{empId}`），而非查数据库。

---

#### 3.2.15 OperationLogController — 操作日志（1个接口）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/operation-log/page` | 分页查询操作日志（支持按模块、时间范围筛选） |

---

## 四、Redis 缓存设计

### 4.1 为什么使用 Redis

OA系统存在大量**读多写少**的场景（如报表统计、公告已读状态），以及**实时性临时数据**（如在线用户状态、Token 会话）。这些场景如果全部查数据库，会导致：

1. **报表统计**：涉及多表联查 + 聚合计算（COUNT、GROUP BY），每次打开报表页面都执行一次全量计算，20个员工的考勤聚合查询平均耗时 200-500ms，100人以上会超过 1s。
2. **Token 校验**：每个请求都要验证 Token，如果查数据库每次都要 IO，接口响应时间至少增加 10-50ms。
3. **在线用户**：状态变化极频繁（每次请求都续期），不适合持久化存储。

Redis 作为内存数据库，单次读写耗时在微秒级，能有效解决以上问题。

### 4.2 Redis Key 全景表

| Key 模式 | 数据类型 | TTL | 使用场景 | 使用理由 |
|---------|---------|-----|---------|---------|
| `token:{empId}` | String | 2小时 | Token 会话管理 | 服务端存储 Token 副本，实现主动注销和单设备登录。每次请求拦截器从 Redis 取 Token 与请求头比对，Token 不匹配或已过期则拒绝访问 |
| `online:user:{empId}` | Hash | 30分钟 | 在线用户状态 | 存储用户登录信息（姓名、IP、浏览器、登录时间）。每次请求自动续期 TTL，用户停止操作30分钟后自动标记为离线，无需定时任务清理 |
| `cache:report:personal:{empId}:{month}` | String(JSON) | 10分钟 | 个人考勤报表缓存 | 个人月度考勤汇总是固定数据（历史月份不会变），缓存后同一用户10分钟内重复查看报表直接返回缓存，不查数据库 |
| `cache:report:admin:attendance:{month}` | String(JSON) | 10分钟 | 管理员考勤汇总缓存 | 管理员报表涉及全员数据聚合，计算量是个人报表的数十倍，缓存效果更显著 |
| `cache:report:admin:today` | String(JSON) | 5分钟 | 今日概览缓存 | 今日概览数据变化较频繁（员工持续打卡），TTL 设为5分钟，在数据实时性和性能之间取平衡 |
| `cache:report:admin:dept:{month}` | String(JSON) | 10分钟 | 部门对比缓存 | 部门维度聚合涉及按部门分组统计，缓存后避免重复 GROUP BY 操作 |
| `cache:report:admin:leave:{month}` | String(JSON) | 10分钟 | 请假分析缓存 | 请假数据按类型分组统计（事假/病假/年假等），缓存10分钟足够 |
| `cache:report:admin:ranking:{month}:{type}` | String(JSON) | 10分钟 | 员工排名缓存 | 排名计算需要遍历所有员工考勤数据并排序，是计算量最大的报表 |
| `notice:read:{noticeId}` | Set | 无过期 | 公告已读记录 | 使用 Redis Set 的 `SISMEMBER` 命令 O(1) 判断某用户是否已读。如果用数据库表存储，每次打开公告列表都需要 JOIN 查询 |
| `rate:login:{ip}` | String(计数器) | 60秒 | 登录限流 | 使用 Redis 的 `INCR` 原子操作实现滑动窗口限流，同一IP 60秒内最多5次登录请求，防止暴力破解 |

### 4.3 Redis 使用模式详解

#### 模式一：Token 会话管理

```
登录 → token:{empId} = accessToken (TTL 2h)
每次请求 → 拦截器从 Redis 取 Token 比对
登出 → 删除 token:{empId}
```

**为什么不用 JWT 自身验证（无状态模式）**：纯 JWT 无状态模式下，Token 一旦签发就无法撤销。如果用户登出后 Token 仍在有效期内，攻击者可以使用该 Token 冒充用户。将 Token 存入 Redis 后，登出时删除即可使 Token 立即失效。

#### 模式二：报表缓存（Cache-Aside）

```
查询报表 → 先查 Redis 缓存
  命中 → 直接返回（微秒级）
  未命中 → 查数据库聚合 → 结果写入 Redis（TTL 10min）→ 返回
考勤数据变更 → 清除相关缓存 Key
```

**为什么 TTL 设为10分钟**：报表数据对实时性要求不高（管理者查看报表通常是为了了解趋势，不要求秒级精确），10分钟的缓存窗口在数据新鲜度和数据库压力之间取得平衡。今日概览设为5分钟是因为当天数据变化更频繁。

#### 模式三：公告已读（Redis Set）

```
用户打开公告 → SADD notice:read:{noticeId} {empId}
判断是否已读 → SISMEMBER notice:read:{noticeId} {empId} (O(1))
```

**为什么不用数据库表**：假设有100条公告、20个员工，数据库表需要2000条记录。每打开公告列表页面，需要 LEFT JOIN 这张表判断每条公告的已读状态。而 Redis Set 的 `SISMEMBER` 时间复杂度 O(1)，无论数据量多大都能微秒级返回。

#### 模式四：在线用户（Redis Hash + TTL 续期）

```
登录 → HSET online:user:{empId} empName ip browser loginTime + EXPIRE 30min
每次请求 → EXISTS + EXPIRE（续期）
登出 → DEL online:user:{empId}
查看在线用户 → KEYS online:user:* → 批量 HGETALL
```

**为什么 TTL 设为30分钟**：用户30分钟内没有任何操作则视为离线，这是合理的心跳超时时间。每次请求自动续期，活跃用户永远不会超时。相比定时任务轮询数据库判断在线状态，Redis 的 TTL 机制天然实现了自动过期。

#### 模式五：登录限流（INCR + EXPIRE）

```
登录请求到达 → INCR rate:login:{ip}
  返回值 <= 5 → 允许登录
  返回值 > 5 → 返回 429 Too Many Requests
首次 INCR → EXPIRE 60s（设置窗口期）
```

**为什么用 Redis 而不是本地计数器**：`INCR` 是 Redis 的原子操作，天然线程安全。如果用 Java 本地的 AtomicInteger，在多实例部署时无法共享计数。Redis 方案天然支持分布式环境。

### 4.4 缓存一致性策略

系统采用**主动清除**策略保证缓存一致性：

- 考勤数据变更时，调用 `clearPersonalCache(empId, month)` 和 `clearAdminCache(month)` 主动删除对应报表缓存
- 下次查询时缓存未命中，重新从数据库聚合计算并写入新缓存

不使用 Redis 的自动过期策略作为主要一致性手段，因为：用户修改考勤数据后立即查看报表，期望看到最新数据。如果仅依赖 TTL 过期，用户会看到旧数据。

---

## 五、系统架构总结

```
┌─────────────────────────────────────────────────┐
│                    前端 (Vue 3)                   │
│  Element Plus + Tailwind CSS + Vue Router + Pinia │
│                  Vite Dev: 8848                   │
└────────────────────┬────────────────────────────┘
                     │ HTTP (axios)
                     │ Authorization: Bearer <JWT>
                     ▼
┌─────────────────────────────────────────────────┐
│              后端 (Spring Boot 3)                 │
│                                                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐   │
│  │拦截器     │  │Controller│  │AOP切面        │   │
│  │AuthFilter │→ │REST接口   │→ │OperationLog  │   │
│  │RateLimit  │  │65个端点   │  │自动记录日志   │   │
│  └──────────┘  └────┬─────┘  └──────────────┘   │
│                      │                             │
│              ┌───────┴───────┐                    │
│              │   Service层   │                    │
│              │  业务逻辑处理  │                    │
│              └───┬───────┬───┘                    │
│                  │       │                         │
│          ┌───────┘       └───────┐                │
│          ▼                       ▼                │
│   ┌─────────────┐      ┌──────────────┐          │
│   │  MySQL 8.0  │      │    Redis     │          │
│   │  13张业务表  │      │  Token/缓存   │          │
│   └─────────────┘      └──────────────┘          │
│                                                    │
│                  Port: 8080                        │
└─────────────────────────────────────────────────┘
```

---

## 六、数据统计与报表 SQL 详解

系统的统计和报表功能集中在 `ReportServiceImpl`（个人报表 + 管理员报表）和 `StatisticsServiceImpl`（数据看板仪表盘）两个 Service 中。由于使用 MyBatis-Plus 的 `LambdaQueryWrapper` 构建 SQL，下面将 Java 代码还原为对应的等效 SQL 语句，逐个分析其统计逻辑。

---

### 6.1 个人报表（ReportServiceImpl）

个人报表面向普通员工，提供4类统计数据。

#### 6.1.1 个人月度考勤汇总

**前端页面**：个人报表页 — 仪表盘 + 统计卡片

**业务逻辑**：查询指定员工在指定月份的所有考勤记录，按 `status` 字段分组统计正常、迟到、早退、缺勤天数，并计算出勤率。

**等效 SQL**：

```sql
-- 查询某员工某月全部考勤记录
SELECT id, emp_id, work_date, clock_in, clock_out, status, remark
FROM oa_attendance
WHERE emp_id = #{empId}
  AND work_date BETWEEN '2026-05-01' AND '2026-05-31';
```

**Java 层统计逻辑**（在内存中对结果集分组计数）：

```java
// 遍历记录，按 status 分类统计
switch (a.getStatus()) {
    case 0 -> normalDays++;       // 正常
    case 1 -> lateDays++;         // 迟到
    case 2 -> earlyLeaveDays++;   // 早退
    case 3 -> absentDays++;       // 缺勤
}

// 出勤率 = 正常天数 / 月总天数 × 100%
attendanceRate = Math.round((double) normalDays / totalDays * 10000) / 100.0;
```

**设计理由**：此处未使用 `GROUP BY status` 让数据库聚合，而是在 Java 层遍历统计。原因是：数据量小（单员工单月最多31条记录），数据库聚合的 SQL 生成 + 网络传输开销反而大于内存遍历。且后续需要计算 `attendanceRate`，在 Java 层处理更灵活。

---

#### 6.1.2 个人出勤趋势

**前端页面**：个人报表页 — 折线图

**业务逻辑**：查询最近 N 个月（默认6个月）每月的出勤率，形成趋势折线。

**等效 SQL**（循环执行6次，每个月一次）：

```sql
-- 逐月查询（以2026-05为例）
SELECT id, emp_id, work_date, clock_in, clock_out, status, remark
FROM oa_attendance
WHERE emp_id = #{empId}
  AND work_date BETWEEN '2026-05-01' AND '2026-05-31';
```

**统计方式**：复用 6.1.1 的 `getPersonalAttendanceSummary` 方法，循环调用6次，每次获取一个月的 `attendanceRate`，组成趋势数组。

**设计理由**：为什么不写一条 SQL 用 `GROUP BY MONTH(work_date)` 完成所有月份的统计？因为每次调用 `getPersonalAttendanceSummary` 时会先查 Redis 缓存（`cache:report:personal:{empId}:{month}`），如果命中缓存则直接返回，不查数据库。循环调用可以充分利用缓存，避免重复计算。

---

#### 6.1.3 个人请假统计

**前端页面**：个人报表页 — 饼图

**业务逻辑**：统计指定月份内该员工已通过的请假记录，按请假类型分组。

**等效 SQL**：

```sql
SELECT id, emp_id, leave_type, start_time, end_time, reason, status
FROM oa_leave_apply
WHERE emp_id = #{empId}
  AND start_time >= '2026-05-01 00:00:00'
  AND end_time <= '2026-05-31 23:59:59'
  AND status = 2;  -- 2=已通过
```

**Java 层分组**：

```java
// 按请假类型分组计数：{ "0": 3, "1": 1, "2": 2 } → 事假3天、病假1天、年假2天
Map<String, Long> leaveByType = leaves.stream()
    .collect(Collectors.groupingBy(l -> String.valueOf(l.getLeaveType()), Collectors.counting()));
```

**设计理由**：`status = 2` 只统计已通过的请假，待审批和已拒绝的不计入。时间条件使用 `start_time >= 月初 AND end_time <= 月末` 确保只取完全在本月内的请假记录。

---

#### 6.1.4 个人月度对比

**前端页面**：个人报表页 — 对比柱状图

**业务逻辑**：将本月与上月（当前月 - 1）的正常天数、迟到天数、缺勤天数进行对比。

**统计方式**：调用两次 `getPersonalAttendanceSummary`，一次传入本月，一次传入上月，对比返回值。

**等效 SQL**（执行两次）：

```sql
-- 本月
SELECT ... FROM oa_attendance WHERE emp_id = #{empId} AND work_date BETWEEN '2026-05-01' AND '2026-05-31';
-- 上月
SELECT ... FROM oa_attendance WHERE emp_id = #{empId} AND work_date BETWEEN '2026-04-01' AND '2026-04-30';
```

---

### 6.2 管理员报表（ReportServiceImpl）

管理员报表面向管理层，涉及全员数据聚合，计算量远大于个人报表。

#### 6.2.1 全员考勤汇总

**前端页面**：管理员报表页 — 统计卡片 + 仪表盘

**业务逻辑**：查询指定月份所有员工的考勤记录，统计正常/迟到/早退/缺勤总次数和平均出勤率。

**等效 SQL**：

```sql
-- 查询全员某月全部考勤记录（不做GROUP BY，全部加载到内存）
SELECT id, emp_id, work_date, clock_in, clock_out, status, remark
FROM oa_attendance
WHERE work_date BETWEEN '2026-05-01' AND '2026-05-31';
```

**Java 层统计**：

```java
// 全量加载后在内存中过滤计数
long normalCount = records.stream().filter(a -> a.getStatus() == 0).count();
long lateCount   = records.stream().filter(a -> a.getStatus() == 1).count();
long earlyCount  = records.stream().filter(a -> a.getStatus() == 2).count();
long absentCount = records.stream().filter(a -> a.getStatus() == 3).count();

// 平均出勤率 = 正常记录数 / 总记录数 × 100%
avgRate = Math.round((double) normalCount / total * 10000) / 100.0;
```

**设计理由**：为什么不使用 `SELECT status, COUNT(*) FROM oa_attendance WHERE ... GROUP BY status`？因为管理员报表后续可能需要更多维度的分析（按部门、按员工），全量加载到内存后可以复用同一份数据做不同维度的统计，避免多次查询数据库。且20个员工×22个工作日≈440条记录，内存完全可承受。

---

#### 6.2.2 部门出勤对比

**前端页面**：管理员报表页 — 柱状图（按部门分组）

**业务逻辑**：逐个部门查询该部门所有员工在指定月份的考勤记录，计算每个部门的出勤率。

**等效 SQL**（循环执行，每个部门一组）：

```sql
-- 第1步：查询所有部门
SELECT id, dept_name, parent_id, sort, leader, phone, status FROM sys_dept;

-- 第2步：查询某部门下的所有在职员工
SELECT id, emp_code, emp_name, dept_id FROM sys_employee WHERE dept_id = #{deptId} AND status = 1;

-- 第3步：查询这些员工在该月的考勤记录
SELECT id, emp_id, work_date, clock_in, clock_out, status
FROM oa_attendance
WHERE emp_id IN (#{empId1}, #{empId2}, ...)
  AND work_date BETWEEN '2026-05-01' AND '2026-05-31';
```

**Java 层计算**：

```java
// 每个部门独立计算
long normal = records.stream().filter(a -> a.getStatus() == 0).count();
double rate = (double) normal / records.size() * 100;  // 出勤率
```

**设计理由**：采用"先查部门 → 查部门员工 → 查员工考勤"的三步走策略，而非一条复杂的 JOIN + GROUP BY SQL。原因是：部门数量通常只有5-10个，每次查询数据量可控。且 MyBatis-Plus 的 `LambdaQueryWrapper` 不方便构建 `IN` 子查询，分步查询代码更清晰。

**如果用一条 SQL 实现**：

```sql
SELECT d.dept_name,
       COUNT(*) AS total_records,
       SUM(CASE WHEN a.status = 0 THEN 1 ELSE 0 END) AS normal_count,
       ROUND(SUM(CASE WHEN a.status = 0 THEN 1 ELSE 0 END) / COUNT(*) * 100, 2) AS rate
FROM sys_dept d
JOIN sys_employee e ON e.dept_id = d.id AND e.status = 1
JOIN oa_attendance a ON a.emp_id = e.id
WHERE a.work_date BETWEEN '2026-05-01' AND '2026-05-31'
GROUP BY d.id, d.dept_name;
```

系统未采用此方案，因为分步查询可以利用 Redis 缓存中间结果（如员工列表），且代码可读性更好。

---

#### 6.2.3 全员出勤趋势

**前端页面**：管理员报表页 — 折线图

**业务逻辑**：查询最近 N 个月（默认12个月）每月的全员平均出勤率。

**等效 SQL**（循环执行12次）：

```sql
-- 以2026-05为例
SELECT id, emp_id, work_date, clock_in, clock_out, status
FROM oa_attendance
WHERE work_date BETWEEN '2026-05-01' AND '2026-05-31';
```

**统计方式**：复用 `getAdminAttendanceSummary` 方法，循环12次，取出每月的 `avgAttendanceRate`。

---

#### 6.2.4 请假类型分析

**前端页面**：管理员报表页 — 饼图

**业务逻辑**：统计指定月份内已通过的请假记录，按请假类型（事假/病假/年假等）分组计数。

**等效 SQL**：

```sql
SELECT id, emp_id, leave_type, start_time, end_time, reason, status
FROM oa_leave_apply
WHERE start_time >= '2026-05-01 00:00:00'
  AND end_time <= '2026-05-31 23:59:59'
  AND status = 2;  -- 只统计已通过的
```

**Java 层分组**：

```java
// 按类型分组：{ "0": 5, "1": 3, "2": 2 } → 事假5次、病假3次、年假2次
Map<String, Long> byType = leaves.stream()
    .collect(Collectors.groupingBy(l -> String.valueOf(l.getLeaveType()), Collectors.counting()));
```

**前端类型映射**（在 Vue 中完成）：

```javascript
{ "0": "事假", "1": "病假", "2": "年假", "3": "婚假", "4": "产假", "5": "其他" }
```

---

#### 6.2.5 员工出勤排名

**前端页面**：管理员报表页 — 排名表格

**业务逻辑**：查询所有在职员工在指定月份的考勤记录，计算每人的出勤率，按出勤率排序取前10名（红榜）或后10名（黑榜）。

**等效 SQL**（循环执行，每个员工一次）：

```sql
-- 第1步：查询所有在职员工
SELECT id, emp_code, emp_name FROM sys_employee WHERE status = 1;

-- 第2步：逐个查询考勤记录
SELECT id, emp_id, work_date, clock_in, clock_out, status
FROM oa_attendance
WHERE emp_id = #{empId}
  AND work_date BETWEEN '2026-05-01' AND '2026-05-31';
```

**Java 层计算与排序**：

```java
// 每个员工独立计算出勤率
long normal = records.stream().filter(a -> a.getStatus() == 0).count();
double rate = (double) normal / records.size() * 100;

// 排序：best=从高到低，worst=从低到高
rankings.sort((a, b) -> Double.compare(b.getRate(), a.getRate()));

// 取前10名
List<Map<String, Object>> top10 = rankings.stream().limit(10).collect(Collectors.toList());
```

**如果用一条 SQL 实现**：

```sql
SELECT e.id AS emp_id, e.emp_name,
       COUNT(*) AS total_days,
       SUM(CASE WHEN a.status = 0 THEN 1 ELSE 0 END) AS normal_days,
       ROUND(SUM(CASE WHEN a.status = 0 THEN 1 ELSE 0 END) / COUNT(*) * 100, 2) AS rate
FROM sys_employee e
JOIN oa_attendance a ON a.emp_id = e.id
WHERE e.status = 1
  AND a.work_date BETWEEN '2026-05-01' AND '2026-05-31'
GROUP BY e.id, e.emp_name
ORDER BY rate DESC
LIMIT 10;
```

**为什么不用一条 SQL**：分步查询的每个中间结果都有 Redis 缓存。当多个管理员同时查看报表时，第二次查询直接命中缓存，不查数据库。而一条复杂 SQL 无法利用中间缓存。

---

#### 6.2.6 今日打卡概览

**前端页面**：数据看板页 — 仪表盘 Gauge 图

**业务逻辑**：统计今日的打卡人数、未打卡人数、迟到人数。

**等效 SQL**：

```sql
-- 在职员工总数
SELECT COUNT(*) FROM sys_employee WHERE status = 1;

-- 今日全部考勤记录
SELECT id, emp_id, work_date, clock_in, clock_out, status
FROM oa_attendance
WHERE work_date = CURDATE();

-- Java 层统计：
-- clockedIn = 记录中 clock_in 不为空的条数
-- late = 记录中 status = 1 的条数
-- notClockedIn = 总员工数 - clockedIn
```

**设计理由**：今日概览的 Redis 缓存 TTL 设为5分钟（而非报表的10分钟），因为员工在持续打卡，数据变化较频繁。5分钟在数据实时性和性能之间取平衡。

---

### 6.3 数据看板仪表盘（StatisticsServiceImpl）

数据看板是管理员首页的核心组件，一次请求返回多个维度的统计数据。

#### 6.3.1 员工总数

**等效 SQL**：

```sql
SELECT COUNT(*) FROM sys_employee WHERE status = 1;
```

**说明**：只统计在职员工（status=1），离职/禁用员工不计数。

---

#### 6.3.2 今日考勤统计

**等效 SQL**：

```sql
-- 今日已打卡人数（上班已签到）
SELECT COUNT(*) FROM oa_attendance
WHERE work_date = CURDATE()
  AND clock_in IS NOT NULL;

-- 今日迟到人数（打卡时间晚于09:00）
SELECT COUNT(*) FROM oa_attendance
WHERE work_date = CURDATE()
  AND clock_in > '2026-05-17 09:00:00';

-- 今日早退人数（下班时间早于18:00）
SELECT COUNT(*) FROM oa_attendance
WHERE work_date = CURDATE()
  AND clock_out IS NOT NULL
  AND clock_out < '2026-05-17 18:00:00';

-- 缺勤人数 = 总员工数 - 已打卡人数
```

**设计理由**：迟到判断使用 `clock_in > 当日09:00`，而非 `status = 1`。这是因为 `status` 是打卡时写入的快照值，而仪表盘可能需要实时判断（如管理员手动修正了打卡时间后）。但当前实现中，考勤记录的 `status` 是打卡时一次性写入的，后续不会变更。

---

#### 6.3.3 请假统计

**等效 SQL**：

```sql
SELECT id, emp_id, leave_type, start_time, end_time, reason, status
FROM oa_leave_apply
WHERE start_time <= '2026-05-17 23:59:59'  -- 请假开始时间在本周期结束前
  AND end_time >= '2026-05-01 00:00:00';    -- 请假结束时间在本周期开始后
```

**Java 层统计**：

```java
// 总请假数
leave.put("total", leaveList.size());
// 待审批数（status=0）
leave.put("pending", leaveList.stream().filter(l -> l.getStatus() == 0).count());
// 已通过数（status=1）
leave.put("approved", leaveList.stream().filter(l -> l.getStatus() == 1).count());
// 按类型分组：事假/病假/年假...
Map<String, Long> byType = leaveList.stream()
    .collect(Collectors.groupingBy(OaLeaveApply::getLeaveType, Collectors.counting()));
```

**设计理由**：时间条件使用交叉判断（`start_time <= 结束 AND end_time >= 开始`）而非包含判断。例如一个从4月28日到5月3日的请假，在查看5月请假时也应该出现，因为5月1-3日属于5月。

---

#### 6.3.4 部门人数分布

**前端页面**：数据看板页 — 饼图

**等效 SQL**：

```sql
-- 查询所有部门
SELECT id, dept_name FROM sys_dept;

-- 查询所有在职员工
SELECT id, dept_id FROM sys_employee WHERE status = 1;

-- Java 层按 dept_id 分组统计：
-- { 技术部: 5, 市场部: 3, 财务部: 2, ... }
Map<Long, Long> deptCountMap = employees.stream()
    .collect(Collectors.groupingBy(SysEmployee::getDeptId, Collectors.counting()));
```

---

#### 6.3.5 出勤率趋势（最近7天）

**前端页面**：数据看板页 — 折线图

**等效 SQL**（循环执行7次）：

```sql
-- 以2026-05-15为例
SELECT COUNT(*) FROM oa_attendance
WHERE work_date = '2026-05-15'
  AND clock_in IS NOT NULL;

-- 出勤率 = 当日打卡人数 / 总员工数 × 100
```

**Java 层逻辑**：

```java
for (int i = 6; i >= 0; i--) {
    LocalDate date = today.minusDays(i);
    Long dayClocked = attendanceMapper.selectCount(...);  // 当日打卡人数
    int rate = (int)(dayClocked * 100 / employeeTotal);   // 出勤率
    trend.add(Map.of("date", "05-15", "rate", 95));
}
```

**设计理由**：数据看板的趋势默认展示最近7天，而非报表页的月度趋势。7天的粒度更细，管理员可以看到每日的出勤率波动。

---

### 6.4 统计查询的共性特征

| 特征 | 说明 |
|------|------|
| **查询框架** | 全部使用 MyBatis-Plus `LambdaQueryWrapper`，不手写 SQL XML |
| **聚合方式** | 先 `selectList` 全量加载，再 Java Stream 分组统计（而非 SQL `GROUP BY`） |
| **缓存策略** | 所有报表结果都写入 Redis，Key 包含维度参数（empId/month/type），TTL 5-10分钟 |
| **缓存一致性** | 考勤数据变更时主动清除相关缓存（`clearPersonalCache` / `clearAdminCache`） |
| **日期处理** | 统一使用 `YearMonth.parse(month)` 解析月份，`atDay(1)` 取月初，`atEndOfMonth()` 取月末 |
| **出勤率公式** | `正常记录数 / 总记录数 × 100%`（基于考勤记录数而非工作日数） |
| **排序逻辑** | Java 层 `Comparator` 排序，支持 `best`（降序）和 `worst`（升序）两种排名模式 |

---

## 七、端口与访问

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端开发服务器 | http://localhost:8848 | Vite Dev Server |
| 后端 API 服务 | http://localhost:8080 | Spring Boot |
| API 文档 | http://localhost:8080/doc.html | Knife4j (Swagger) |
