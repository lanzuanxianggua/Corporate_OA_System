spring:
  datasource:
    url: jdbc:mysql://localhost:3306/oa_system_test?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: test_password
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

jwt:
  secret: test-jwt-secret-for-ci-only
